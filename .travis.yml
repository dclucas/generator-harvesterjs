language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-harvesterjs' ]; then cd .. && eval "mv $currentfolder generator-harvesterjs" && cd generator-harvesterjs; fi
